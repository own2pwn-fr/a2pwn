# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# uv
# (uv.lock IS committed)

# a2pwn runtime artifacts
runs.db
*.har
out/
runs/
.a2pwn/
# In-sandbox helpers write their results under the cwd: ~/.local/share and ~/.cache are mounted
# READ-ONLY inside burpwn's sandbox and /tmp there is a private tmpfs the host cannot read.
.a2pwn-browser/
.a2pwn-ws/

# skill index (generated from frontmatter)
skills/_index.json
skills/_index.sqlite

# tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# design scratch (not part of the package)
.design/

# editors / OS
.idea/
.vscode/
*.swp
.DS_Store
